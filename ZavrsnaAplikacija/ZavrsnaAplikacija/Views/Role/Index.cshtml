@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>
@{
    ViewBag.Title = "Add Role";
}
<h2 class="my-2"> Create User Roles</h2>
@*<ul class="list-group">
    @foreach (var item in Model)
    {
        <li class="list-group-item list-group-item-action active">@item.Name</li>
    }
</ul>
<h3>   @Html.ActionLink("Click to Create New Role", "Create", "Role") </h3>*@

<div class="row">
    <div class="col-md-6">
        <ul class="list-group">
            @foreach (var item in Model)
            {
                <li class="list-group-item list-group-item-action active">@item.Name</li>
            }
        </ul>
    </div>
    <div class="col-md-6 py-5">
        <h5>   @Html.ActionLink("Click to Create New Role", "Create", "Role") </h5>
    </div>
</div>

